# Env setup

- Copy `config/env/.env.example` to `config/env/.env`.
- Set `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` to a key with billing enabled and these APIs turned on:
  - Maps JavaScript API
  - Places API

`config/env/.env` is gitignored and should not be committed.

